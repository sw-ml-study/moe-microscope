# F14 reproducer, resolved upstream (61da67ae): constant constructors (fill, zeros, ones) are constant leaves inside grad.
W = param[3]
W = [1, 2, 3]
print("fill-in-grad gradient", grad(reduce_add(W * fill([3], 2)), W))
