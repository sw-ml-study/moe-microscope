# F14 reproducer: constant constructors (fill, zeros) are rejected inside grad; checked by scripts/run-probes.
W = param[3]
W = [1, 2, 3]
print("eager", reduce_add(W * fill([3], 2)))
print("grad", grad(reduce_add(W * fill([3], 2)), W))
