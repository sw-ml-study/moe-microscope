# F9 reproducer, resolved upstream: embed accepts the rank-two [B, T] token input the reference documents.
emb = embed(8, 4, 0)
print("batched embed shape", shape(apply(emb, [[1, 2, 3], [4, 5, 6]])))
