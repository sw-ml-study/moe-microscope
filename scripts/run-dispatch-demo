#!/bin/sh
# Run SD01 into a temporary directory; check (or write) its diagrams and the deterministic columns of its results row.
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
mlpl=$("$repo_root/scripts/select-mlpl")
mode=${1:-check}
. "$repo_root/scripts/gate-cache"
gate_cache_start "$mode" run-dispatch-demo \
    demos/dispatch_microscope.mlpl assets/previews/dispatch-combine.svg \
    "row:docs/reference/results.md:| SD01 |"
tmp_rel="tmp/dispatch-demo"
mkdir -p "$repo_root/$tmp_rel"
binary=$("$mlpl" --version 2>/dev/null | head -1 | tr -d '\n')

"$mlpl" --source-dir "$repo_root" "$repo_root/demos/dispatch_microscope.mlpl" -- "$tmp_rel" "$binary"

for name in dispatch-combine; do
    generated="$repo_root/$tmp_rel/$name.svg"
    committed="$repo_root/assets/previews/$name.svg"
    [ -s "$generated" ] || { echo "dispatch demo did not write $name.svg" >&2; exit 1; }
    if [ "$mode" = "write" ]; then
        cp "$generated" "$committed"
    elif ! cmp -s "$generated" "$committed"; then
        echo "$name.svg preview is stale; run scripts/run-dispatch-demo write" >&2
        exit 1
    fi
done

# The row's deterministic columns (everything except binary and ms/token) must match docs/reference/results.md.
row=$(cat "$repo_root/$tmp_rel/results-row.md")
key=$(printf '%s\n' "$row" | awk -F'|' '{ $4=""; $12=""; print }')
if [ "$mode" = "write" ]; then
    grep -q '^| SD01 |' "$repo_root/docs/reference/results.md" && sed -i '' '/^| SD01 |/d' "$repo_root/docs/reference/results.md"
    printf '%s\n' "$row" >> "$repo_root/docs/reference/results.md"
else
    committed_key=$(grep '^| SD01 |' "$repo_root/docs/reference/results.md" | awk -F'|' '{ $4=""; $12=""; print }')
    [ "$key" = "$committed_key" ] || {
        echo "SD01 results row differs from docs/reference/results.md; run scripts/run-dispatch-demo write" >&2
        echo "generated: $key" >&2
        echo "committed: $committed_key" >&2
        exit 1
    }
fi
rm -rf "$repo_root/$tmp_rel"
gate_cache_record "$mode"
echo "PASS sparse dispatch contract, previews, and results row ($mode)"
