SS02 selective state-space block. The SS01 block with A, B, and C computed from the token (a gated, Mamba-like block: A(x_t) = sigmoid(x_t Wa), B(x_t) = x_t Wb, C(x_t) = x_t Wc, then the output projection), same budget and process discipline. Measure against SS01 and DN01: validation loss and family accuracy; the decay per position on one arithmetic and one prose window drawn side by side (what the block forgets and where); state bytes unchanged from SS01. Row SS02, diagrams (the selective gates on one window; SS01 against SS02 against DN01), docs/experiments/SS02.md, tests over the gates' dependence on the input, catalog and recording.