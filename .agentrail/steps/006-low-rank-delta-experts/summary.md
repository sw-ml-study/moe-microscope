LD01 delivered: sixteen rank-4 delta experts packed in two matrices (exact parity with a per-expert loop, tested), shared always-on FFN, deltas-only variant, comparison with DN01 and MX01 via a new results-table reader, router multiply-adds per token recorded, family map over sixteen experts, two diagrams, two results rows; shared+deltas val loss 3.46 and deltas-only 3.37, both better than DN01/MX01 with fewer params than MX01. Committed and pushed.