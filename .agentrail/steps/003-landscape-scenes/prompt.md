Landscape scenes. Write fixtures/landscape/scenes-v0.json from the storyboard: tokens and embedding, attention (dense-baseline-run-v0 attention map), the Engram lookup (engram-run-v0 rows, retrieved values, gate on the example window), the router and experts (walkthrough-v0 logits, probabilities, mask, groups, expert rows, dense and sparse outputs), the recurrence loop (recurrent-moe-run-v0 route strip and loads per recurrence), and the composition (recurrent-moe-engram-run-v0 route and gate strips), with captions that name the source observation and the lesson report, and the parallel call-outs the storyboard justified. Placeholders for the cache, decode-cache compression, and multi-token prediction are drawn as labeled empty stations with the planned lesson id, never with invented values. If any scene needs a value no recording holds, add an MLPL exporter (demos/landscape_export.mlpl, deterministic, pinned like the walkthrough fixture) rather than computing in the browser. Deploy, verify the published page with ?verify=1 in headless Chrome, record it in docs/results/live-demo.md, and link the landscape from the live demo index page and the README.