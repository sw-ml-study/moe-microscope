SD01 sparse dispatch delivered: compress-gather per expert, one evaluation per expert on its sub-batch, gate scaling, selection-matrix scatter back (lib/moe.mlpl helpers with tests). Exact parity (max diff 0) with the dense-masked path over 120 windows; row evaluations 3,360 vs 13,440; parameters per token 3,064 vs 6,280. Interpreter timing honestly slower for sparse at this size and recorded only in the results row. Diagram, catalog, run script in just check. Committed and pushed.