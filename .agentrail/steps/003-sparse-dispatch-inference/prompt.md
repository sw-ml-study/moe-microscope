Forward-only sparse dispatch: evaluate only the selected experts per token, assert parity with the dense-masked path on the same fixture, and count expert evaluations per token and bytes per token as the first speed measurement that differs between architectures. Record moe/dispatch/* observations and draw the dispatch and combine transformation diagram with real values. Tests first; extend just check.