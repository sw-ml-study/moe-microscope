SS01 landed and pushed (53a8c1e). lib/ssm.mlpl: diagonal linear scan s_t = sigmoid(A)*s_(t-1) + x_t B, y = s C, one repeat per position (count traces through grad). demos/ssm_block.mlpl: DN01 with the scan for attention at matched size (1,056 vs 1,024 params), DN01 budget, one process. Row SS01: val loss 4.56 vs DN01 3.79, held-out 0/0/0.286/0.667, train exact match 0.99 vs 0.70, decay 0.26..0.98 mean 0.76. New results column (persistent state bytes at 16/1024 history): SS01 256/256, DN01 4096/262144, dash elsewhere; lib/results.mlpl + test updated. Prefill us/token measured and pinned in fixtures/ssm/state-bytes-v0.json: attention 2.3..16.0, scan 17.7..42.3 (slower, stated honestly). Three diagrams (ssm-structure, ssm-window fading chips, ssm-bytes), recording state-space-run-v0.json pinned (13 frames, 319 values) and replayed in the live demo (headless verify: 13 frames, 33 observations, recorded in docs/results/live-demo.md), catalog entry, docs/experiments/SS01.md, concept page with measured numbers, finding 11, report card rank 13, host handoff shapes, README claim 10, saga queue. Tests: zero decay forgets, unit decay accumulates, gradients to A/B/C, byte accounting, short training run. Full gate passed (2014 s; the sibling interpreter was rebuilt four times today so the cache invalidated as designed). Landscape scene for the state chip left to hybrid-close (step 008) as planned. Step 003 literate-org-document inserted at the user's request.