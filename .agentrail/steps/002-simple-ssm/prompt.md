SS01 simple state-space block. lib/ssm.mlpl: a diagonal linear state-space block written out, s_t = A * s_(t-1) + B x_t and y_t = C s_t with a learned diagonal A held in 0 to 1 through a sigmoid, B and C tiny learned projections, the scan a literal-count repeat over the 28-position window (probes/c1_selective_scan_in_grad.mlpl shows the form traces and trains), followed by the residual, in place of DN01's attention block. demos/ssm_block.mlpl trains it on the language domain with the DN01 budget (300 epochs, 90 windows, one process), records the state vector evolving along one window (ssm/state/example [T, dm]), the decay per dimension (ssm/decay), losses, accuracies, and writes row SS01 with params, active params, and a new first-class column: persistent state bytes per token history, measured on the generation benchmark for prompt lengths 16, 64, 256, and 1,024 for both DN01 (key-value cache: grows) and SS01 (fixed), with microseconds per token beside it, honest if the interpreter's sequential scan is slower. Diagrams: the state block data structure with shapes and bytes; the state along one window (a fading state chip per position); state bytes against prompt length for attention and the scan. docs/experiments/SS01.md answers whether a fixed-size state learned anything here and what it costs; docs/concepts/state-space.md gains the measured numbers; tests over the scan (a zero decay forgets, a unit decay accumulates, gradient flows to A, B, and C), the state-bytes accounting, and a short training run; catalog entry, recording pinned, results columns extended with the state-bytes column across every row (dash where not measured).