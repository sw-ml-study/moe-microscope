Emacs org-mode literate, reproducible-research document for the microscope using sw-MLPL's org-babel backend ob-mlpl (../sw-mlpl/elisp/ob-mlpl.el; examples in ../sw-mlpl/examples/literate/*.org and ../demo-coding-agent/docs/mlplcode.org). docs/literate/moe-microscope.org: the setup block that loads ob-mlpl from the adjacent checkout and selects the interpreter (GUI Emacs has a minimal PATH); a primer of small self-contained runnable mlpl blocks (:results output :exports both) walking the reader through the microscope's mechanisms in the lesson order (domain windows, dense block, router and top-k, dispatch parity, recurrence, Engram hashing, the SS01 scan) with the recorded numbers beside them; and a literate reading of the lesson sources that tangles to the committed lib/ and demos/ files byte for byte, proven by scripts/check-tangle in the gate (a block cannot include a neighbour, so the runnable blocks are self-contained and the function blocks are for reading and tangling). Document how to evaluate (C-c C-c), export (C-c C-e h o) with :eval no-export, and where the exported HTML is published (learn/ or docs/). README build-and-run and docs/README link it; the org file passes the doc-link check.