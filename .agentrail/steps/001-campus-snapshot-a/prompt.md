CD00 campus snapshot A and the docent corpus. Author fixtures/campus/snapshot-a.json in the sw-campus Place shape (id, kind, title, tagline, summary, status, children, links) plus a docent block per place (aliases, concepts, example queries, and two or three canned stories each with id, title, text, concepts, kind in arrival/feature/detail/anecdote) for the campus, the Computer History Museum, the IBM 1130 wing and its 1130 exhibit, the APL exhibit under a programming-languages wing of the Computer Science building, and the RCA 1802 exhibit under its museum wing; exclude the 1442 and radio on purpose (v1's new-knowledge event). Locate the APL and RCA 1802 demo repositories and record their URLs, or mark them placeholder as sw-campus does. In lib/campus.mlpl: load the snapshot, derive the URL of a place from its ancestor chain, a catalog hash, the deterministic corpus generator (per-place positives from aliases, concepts, and example queries; navigation, explain, recommend, story-eliciting, deliberately ambiguous, and out-of-domain rows; intent labels navigate/explain/recommend/story/unsupported and destination labels for the three exhibits plus none), a seeded split, and the story-selection policy (untold story for the current place first, then for the predicted destination or concept; arrival story once; no repeat unless asked; offer another while untold remain; cleared context starts over) with tests over scripted visits. demos/campus_corpus.mlpl draws the what/why/how diagrams (snapshot tree with docent blocks, one place expanded into its generated rows with labels, the split, the story policy on a scripted visit) to assets/previews/campus-*.svg, writes fixtures/campus/corpus-a.json, and reports counts and bytes (catalog, corpus). Gate script, catalog entry CD00 (form from-scratch), docs/experiments/CD00.md laboratory report, results row, mlplunit tests over the generator, labels, split, hash, and policy. Sizes are measured: catalog bytes, corpus bytes, rows per intent and destination.
