Restore scripts/run-emit-frame-loops. Since commit 28478d6 (the F16 resolution flip) the file has been a 0-byte script, so the gate's emit-frame connect-path probe passes trivially. Recover the previous content from 28478d6^ (bare include refused, nested includes resolve rooted at the map, parent-relative refused), apply the F16 flip the commit message describes, verify it against the current mlpl-serve, give it a gate-cache key like the other demo gate scripts (demo probes, capture-recording, bundle-program, the mlpl-serve binary), and record the outcome in docs/reference/sw-mlpl-findings.md under F16.