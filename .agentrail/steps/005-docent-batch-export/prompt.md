Docent batch export. Choose the configuration that meets the usefulness bar at the smallest size (record the choice and the numbers in docs/experiments/CD02.md). Add the `just docent` recipe: native batch training of that configuration, export of the packed weights, labels (intents, destinations, concepts), and the manifest (model name, version, trained-from commit, catalog hash, experts, top-k, params, quantization) with write_atomic and to_json under fixtures/campus/model-a/, INT8 projection with measured file sizes, the recorded training run for page replay, and metrics.json. An MLPL inference twin (u:docent_predict over the reloaded export) asserts parity with the trained model on every corpus row. The gate checks the export against the pinned fixture (hashes, manifest fields, parity) without retraining, as the other lessons do. Document the batch update procedure (regenerate corpus, rerun recipe, commit export, bump version) in docs/implementation/docent-export.md with the manifest schema. Results row with the export sizes; the usefulness bar recorded as met or not, per criterion, in docs/results/current-findings.md.
