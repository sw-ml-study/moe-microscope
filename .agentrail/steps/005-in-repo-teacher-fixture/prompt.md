Train the larger in-repo dense teacher (same DN01 source at a larger configuration, e.g. d_model 64, two blocks) once with a fixed seed, export its next-token probability distributions over the training split as a bounded fixture with a documented schema (fixtures/teacher/), record the teacher's own triple and results row, and add a hash and size check for the fixture to just check. Document the fixture schema so the external-teacher export in Saga 4 can produce the same shape. Complete the saga with --done and summarize Saga 2 readiness.