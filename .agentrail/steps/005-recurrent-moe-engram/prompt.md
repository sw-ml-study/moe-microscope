RE01 recurrent MoE plus Engram: all three sparsities together, and the ablation table. demos/recurrent_moe_engram.mlpl composes RC01's recurrence, RM01's routed experts, and EG01's Engram (from-scratch form, the size chosen by EG01) in one model trained with the DN01 budget, and fills the seven-row ablation table from the research: dense (DN01), recurrent (RC01), Engram (EG01), MoE (MX01), recurrent MoE (RM01), MoE plus Engram, recurrent MoE plus Engram (RE01), each with params, active params per token, bytes per token, ms per token, val loss, family accuracies, entropy, balance, specialization, Engram gate mean. The table is written to docs/reference/results.md rows and rendered as a diagram (rows against the quality and cost columns) with a second diagram of RE01's per-recurrence routing and Engram gate on one window. docs/experiments/RE01.md answers "do memory sparsity and compute sparsity complement each other?" with the numbers and their limitations (one seed, 120 examples; cite DS01 for the data effect). Recording pinned; gate script; catalog entry; tests over the composed forward and the ablation writer. Update docs/results/current-findings.md with the recurrence and Engram claims (evidence, interpretation, limitation each) and docs/results/quality.md.
