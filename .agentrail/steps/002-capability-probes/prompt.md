Write native mlplunit probes for the items listed under 'To be probed' in docs/sw-mlpl-blockers.md: adam over a mixed list of models and params; gradient flow through the same model applied R times inside repeat; differentiability of gather_rows and of softmax over masked logits as a top-k gate; existence/signature of a KL-divergence builtin (else write KL from softmax and log); per-token routing loop cost at T=16, E=8 measured with clock_ms; emit_frame behavior inside train blocks. Record each result as supported, awkward, or blocked with evidence in the ledger. Add the smallest observation facade over emit_frame in lib/ with tests. Extend just check with run-tests. Do not implement lessons yet.