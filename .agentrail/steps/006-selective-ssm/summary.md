SS02 landed and pushed (9669a75). The SS01 scan with its decay, input, and read gates computed from the token (a_t = sigmoid(x_t Wa + b), b_t = x_t Wb, c_t = x_t Wc). The decay bias is load-bearing: with it Wa = 0 reproduces SS01 exactly so the selective block contains the fixed one; the first biasless version started every decay at 0.5 and reached 5.86 against 5.63. Two variants, one process each, so the comparison is not confounded by capacity. Matched mixer (1,040 params against SS01's 1,056 and attention's 1,024): validation loss 4.42 against 4.56, with half the persistent state (128 B against 256). Wide (2,080 mixer params, 256 B state): 5.63, the worst of the four mixers, every training row memorized, but held-out prose 1.0, the only mixer to answer every prose prompt. The learned decay sits near 0.8 with a 0.2 range within a window and dips at the equals sign: real but narrow selectivity, shown as decay strips for one arithmetic and one prose window. Rows SS02 and SS02m; two diagrams (strips; four mixers compared, drawn from the committed table); recording pinned and replayed (headless verify 13 frames, 32 observations, recorded in live-demo.md); tests for gate dependence, reduction to the fixed scan under a constant gate, and gradients to every part; docs/experiments/SS02.md, finding 13, report card 15, README claim 11, catalog, the measured concept page, and six new literate blocks so the tangle still reproduces lib/ssm.mlpl. Full gate passed (2235 s, nothing cached: the sibling interpreter was rebuilt again mid-session, now c1251816).