CD02 to CD04 routed docent and specialization. Reuse lib/moe.mlpl: the same encoder behind a router over rank-4 delta experts (LD01 packed form), variants with 4 and 8 experts, top-1, balance weight as in MX01; a top-2 variant for the ambiguity panel. demos/docent_moe.mlpl trains both expert counts in batch, measures the routing distribution per destination and per concept, computes the specialization score, derives expert annotations from the measured distributions (what each expert appears to have learned, never assigned), draws the routing and specialization map, the expert annotation table, and the ambiguity panel (the deliberately ambiguous queries with their destination scores and routed experts under top-1 and top-2), and writes results rows for CD02 (4 experts), CD02 (8 experts), and CD04 (top-2) with params, active params, expert evaluations, entropy, balance, specialization, intent and destination accuracy, top-3, ms per query. Recording of the training run through emit_frame pinned for the generic host. Gate script, catalog entries, docs/experiments/CD02.md CD03.md CD04.md (CD03 is the specialization report), results rows, tests over the docent router path and the annotation derivation. Compare against CD01 in the CD02 report: what the experts bought, and whether the usefulness bar (destination and intent at or above 0.9, ambiguous queries ranked) is met by dense, four, or eight experts.
