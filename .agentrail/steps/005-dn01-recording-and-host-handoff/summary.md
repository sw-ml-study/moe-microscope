DN01 recorded over live mlpl-serve SSE under the peer version-zero schema (13 frames, 29 observations, 96 values), reproduced exactly on a second run, pinned by hash with counts in fixtures/recordings/index-v1.json, validated and live-checked in just check. Loop streaming proven (train and while). F16 (eval_stream has no include/sandbox/args) filed with scripts/bundle-program, inline mixture twin, and guarded writes as workaround; S1 stale adjacent server noted with scripts/build-local-serve and a documented selection order. docs/host-handoff.md written for demo-extensions. D1 and F10 verified fixed upstream, pins flipped. Committed and pushed.