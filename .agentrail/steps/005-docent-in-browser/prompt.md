CD05 and CD06 docent in the browser. Build the docent page under learn/ (index.html, runtime.js adapted from ../demo-abstract-algebra/learn/runtime.js, the published playground loaded lazily in a hidden same-origin iframe on the first query, never on page load; do not copy more than the bridge). The program sent to the WasmSession inlines the exported weights and labels (the WASM surface has no filesystem sandbox) and the inference twin from step 4; the page answers queries with the catalog-backed response (title, breadcrumb, URL, story when the intent is story, "no sufficiently specific destination" and "I only know this campus" when appropriate), the "why this?" panel (router distribution, expert annotation, matched signals), and replays the recorded batch run (per-epoch accuracy and expert utilization). Measure and record as the CD05 row: playground load time, first-query latency, per-query latency (p50, p95). Then the budgeted live-training probe as the CD06 row: time per epoch and for a full train of the chosen configuration through the WasmSession, against the budget of one second per epoch and ten seconds per full train; keep the "teach one epoch" and "teach a new exhibit" controls only if the budget is met, otherwise document the decision and keep replay plus the short fine-tune. Publish on GitHub Pages from this repository (workflow, just recipe, check that learn/ builds from committed fixtures). Update the ledger probes 2 to 4 in docs/reference/sw-mlpl-blockers.md with measured outcomes and any new finding with a reproducer. docs/experiments/CD05.md and CD06.md, catalog entries, results rows.
