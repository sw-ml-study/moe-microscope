Tiered-residency plan landed and pushed (f15bfa9). Saga 14 in docs/overview/plan.md is now quantization, the packed file, and tiered weight residency: the residency unit is a shard record (expert, shared, router, engram, or depth-shard), so one simulator serves the microscope's bank, sw-atlas's depth shards, and a browser's heap/OPFS/range tiers. PK01 gains a ShardDirectoryEntry (id, kind, offset, size, payload, shape, quantization, checksum) and padded records, with the reason stated (536-byte INT4 experts cannot show I/O cost) and the honesty rule (routing and reads real, record sizes chosen). TW01 replaces XC01: tier 0 resident no read, tier 1 a real bounded read_bytes timed with clock_ms, tier 2 the same read plus calibrated latency declared as an estimate; LRU by shards and bytes; capacity curves. Two more steps: replay a foreign manifest read-only by name and hash, and label the trace with true next use to run LRU against the oracle for sw-os-ml gate G4. Five results columns added (shard bytes, hit rate, bytes read per token, load latency, evictions) with M/D/E labels, and a per-token tier/trace observation for the landscape and the generic host. cross-repo-handoffs.md now carries both schemas (ShardManifestEntry we accept, TraceRecord we emit) plus the shared metric names. docs/concepts/expert-cache.md rewritten as the tiered-residency page with a three-tier diagram; saga queue, storyboard placeholder row, experiment index, RB01's forward reference, and the budget demo's generated sentence renamed. Deliberately unchanged: the dated live-demo acceptance record (it records what the page showed) and the research inputs. Full gate passed in 156 s with 29 cached entries, the first run to show the gate cache working (earlier runs all retrained because the sibling interpreter was rebuilt nine times today).