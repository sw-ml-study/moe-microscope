TW01 reframed: tiered weight residency in the plan. Two inputs: sw-atlas's work order (../../software-wrighter-lab/sw-atlas/docs/plan.md section 11: the queued Saga 14 expert-cache work still stands, but Atlas supplies a depth-sharded model, not an expert bank: RAM holds embeddings, shallow encoder, heads; SSD via mmap the deep encoder layers; HDD or file the decoder stack; browser tiers WASM heap or GPU buffer, OPFS or Cache API, server over HTTP Range) and the research note ~/github/software-wrighter-lab/sw-atlas/docs/sw-atlas-research.txt sections 5 to 8 (a weights.bin with a header, shared weights, router, an expert directory of id, offset, size, shape, quantization, checksum, and expert records padded to realistic sizes; three simulated tiers first: resident RAM, SSD/mmap, HDD/file; metrics expert bytes, cache hit percent, bytes read per token, load latency, expert compute, total token latency, evictions; one request drawn as router, RAM hit to compute, SSD miss to read bin to cache to compute; then the browser as another hierarchy compared with MLOS's HDD to SSD to RAM to VRAM). Documentation only, no sibling changes: rewrite Saga 14 in docs/overview/plan.md so PK01 and XC01 become TW01 tiered weight residency: the packed file gains an expert directory and padded records (a scaled storage fixture preserving the exact logical routing, because a 536-byte INT4 expert cannot show I/O cost); residency is measured over three tiers with tier 1 real bounded read_bytes on the packed file timed with clock_ms and tier 2 a calibrated latency model; the residency unit is a shard record that is either an expert (our bank) or a depth shard (a manifest Atlas supplies, consumed by name and hash, never edited here), so the same simulator serves both; the metric vocabulary above becomes the results columns (stored, resident, active, transferred, executed already in RB01 plus hit percent, bytes read per token, load latency, evictions); a per-token residency trace observation tier/trace the landscape's hot-and-cached-experts scene plays; the next-use-labelled trace MLOS gate G4 asks for (known next use against LRU) as a recorded output. Update docs/overview/sagas.md, the landscape storyboard row for XC01, docs/concepts/expert-cache.md status, the sw-atlas section of docs/implementation/cross-repo-handoffs.md (the shard manifest schema we will accept, the trace schema we will emit, the metric names), and docs/reference/README if it lists columns. The doc gate must pass.