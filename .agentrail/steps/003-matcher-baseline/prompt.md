MB01 matcher baseline. Implement in lib/matcher.mlpl the deterministic alias-and-concept matcher the campus mockup uses (pages/index.html in ../../software-wrighter-lab/sw-campus, read only): lowercase query; every place scored by alias substring hits (longest alias first, weighted above) plus concept token overlap; the intent by keyword rules (where/take me/show me -> navigate; what is/tell me about/explain -> explain; like/interested/something about -> recommend; story/anecdote/tale -> story; finished/work/ready/complete/far along -> status); unsupported when no place scores; a ranked destination list. Score it on the validation rows and on fixtures/campus/paraphrases-a.json with the same metrics as CD01 (intent accuracy, destination accuracy, top-3, exhibit accuracy, ambiguous top-2, unsupported recall, paraphrase accuracy and paraphrase intent accuracy) and write the MB01 row to docs/reference/docent-results.md. demos/matcher_baseline.mlpl draws one what/why/how diagram: a paraphrase query beside an alias query, the matcher's scores per place, and where it fails; docs/experiments/MB01.md reports the numbers beside CD01 and states the usefulness-bar margin (docent minus matcher on the paraphrase set, destination and intent) explicitly. Gate script with write/check modes, catalog entry MB01 (implementation from-scratch), tests over the scorer and the intent rules.
