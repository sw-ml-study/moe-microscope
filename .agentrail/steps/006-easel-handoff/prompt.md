Easel handoff and saga close. Write docs/implementation/easel-handoff.md, the implementation-ready work order for ../../software-wrighter-lab/sw-campus: the docent block on Place and the catalog export with hash (from snapshot A), the easel component with passive face (featured exhibit, visit link) and interactive face (ask the docent, why this, open the microscope), lazy loading of the playground session on first use, model and manifest loading with the stale badge, the DocentContext in IndexedDB (current place, recent places, interests, recent queries, told stories) with the story policy and a clear action, the batch update procedure, and the fast path of linking the mockup lobbies to the docent page here. Update cross-repo-handoffs.md to point at it. Pin the docent recordings in fixtures/recordings with the index and budgets, extend the host handoff, and run the recording parity check. Update README (status, learned claims, experiments, what is here), docs/README.md, docs/overview/sagas.md, docs/results/current-findings.md and quality.md with the docent rows, docs/concepts/README.md with the docent as the second training area, the catalog, and the wiki (Learn, Results, Experiments, Internals pages; verify Mermaid and both link kinds after the push). Complete the saga with --done.
