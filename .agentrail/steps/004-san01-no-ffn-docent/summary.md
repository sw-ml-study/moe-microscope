SAN01 landed and pushed (7a819d5). Answer for sw-atlas: removing the docent's feed-forward layer costs nothing on this corpus. SAN01 (hashed, embedding widened 24->25 for a matched 26,094 against CD01's 26,003) scores intent 0.946 / destination 0.934 / paraphrase 0.389 / unsupported recall 0.5 against CD01's 0.938 / 0.925 / 0.296 / 0.3; SAN01b (CD01b's word-vector table, 25,051) scores 0.938 / 0.925 / 0.444 / 0.3 against CD01b's 0.942 / 0.917 / 0.407 / 0.3 and returns the expected ambiguous top-2 pair 0.545 against 0.182. Validation loss falls in both (1.19, 1.17 against 1.50, 1.70). lib/docent.mlpl gained the no-FFN forward/loss for both feature sources, a function-reference evaluator so one lesson scores either, the parameter formula, and a docent-table reader; two diagrams (model with the hidden layer crossed out; six-lesson comparison drawn from the committed table); docs/experiments/SAN01.md, finding 12, report-card 14, README claim 11, catalog entry, and a new sw-atlas section in cross-repo-handoffs carrying the result table and the MOE-RETURN note. Tests in tests/test_noffn.mlpl. Full gate passed (2215 s, everything retrained: the sibling interpreter was rebuilt nine times today, cache invalidating as designed). Limitation recorded everywhere claimed: this docent has no sequence mixing, so it says nothing about attention-only encoders at Atlas's size; matchers still beat every docent on paraphrases. No recording (F16, as for every docent lesson).