SAN01: is no-FFN real? sw-atlas (../../software-wrighter-lab/sw-atlas/docs/plan.md, section 11) bets its architecture on dropping the feed-forward network when the model relies on an external knowledge source, and asks the microscope to check it on the docent corpus before its Saga 5. Strip the hidden layer from CD01 and from CD01b at matched budget: the pooled embedding feeds the intent and destination heads directly (a linear classifier over pooled features); for CD01 widen the hashed embedding so the parameter count matches 26,003 (d=25 gives 26,094; the unused body must not be constructed or counted); for CD01b keep the rank-24 word-vector table (its budget is fixed by the table) and report the smaller count honestly. One process per variant (finding F22). demos/docent_noffn.mlpl writes rows SAN01 (hashed) and SAN01b (word vectors) to docs/reference/docent-results.md with every docent column, a diagram comparing CD01, CD01b, SAN01, SAN01b, MB01, and MB01t on validation intent and destination accuracy, paraphrase accuracy, and unsupported recall, and a recording. A scratch probe on 2026-09-18 gave, for the hashed variant: val loss 1.19 (CD01 1.50), intent 0.946 (0.938), destination 0.934 (0.925), paraphrase destination 0.389 (0.296), unsupported recall 0.5 (0.3); the lesson must reproduce or correct these. docs/experiments/SAN01.md answers the question for sw-atlas (what changed, what did not, and the limitation: a pooled bag of hashed features has no sequence mixing, so this says nothing about attention-only encoders), finding 12 in current-findings, the report card, the docent concept/experiment indices, and a dated entry in docs/implementation/cross-repo-handoffs.md under a new sw-atlas section carrying the SAN01 result and the MOE-RETURN note (MoE stays here as the built-and-measured fallback if no-FFN loses at Atlas's scale). Tests over the no-FFN forward (pooled equals hidden, gradients reach the embedding and heads), gate script run-noffn-demo with gate-cache, justfile recipe, catalog entry, check-structure and check-catalog.