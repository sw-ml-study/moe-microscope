Implement deterministic seeded generators in lib/ for the four task families (small-integer arithmetic, sequence transformations, tiny MLPL expressions checked against the interpreter, short templated prose) with task tags, fixed mixture ratios as a fixture, and train/validation splits over a restricted byte-level alphabet. Implement exact-match oracles and an evaluation harness reporting per-task accuracy, and a results-table writer that appends a row to docs/results.md with the fixed columns. Produce annotated what/why/how diagrams (committed SVG) for the token window, tag, and split data structures. Native mlplunit tests first; extend just check.