Synthetic domain delivered: lib/domain.mlpl (52-symbol alphabet, four seeded task-family generators with exact oracles, mixture fixture of 120 examples with window 28, padded x/y/mask windows, seeded grade_up split), lib/evaluate.mlpl (exact match, per-family accuracy), lib/results.mlpl (21 fixed columns, append). DM01 demo writes three annotated what/why/how diagrams (window, tags, split) from asserted values, freshness-checked in just check; catalog DM01 entry with implementation form and triple notes; check-catalog script. 28 native tests pass. Ledger records rank-one gather, string-list, record_get, and include-path idioms. Committed and pushed.