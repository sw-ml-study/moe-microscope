HA01 the state block with one attention block among several. The Nemotron pattern at microscope scale: state blocks with a single attention block (M M M A), depth from the recurrence machinery of lib/recur.mlpl (the shared block applied R times, deep supervision), so the question is how little attention keeps attention's held-out MLPL answers (the first came from RC01 and RM01). Sweep the position and count of the attention block at fixed parameters, one process per point, rows HA01@<pattern>, with state bytes per token history for the mixed model (fixed states plus the attention cache), a diagram of the patterns against loss and bytes, docs/experiments/HA01.md, tests, catalog, recording of the chosen pattern.