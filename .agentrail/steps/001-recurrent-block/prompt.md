RC01 recurrent block. One physical transformer block (the DN01 block: attention with a residual, FFN with a residual, RMS norm) applied R times to the same token state, with deep supervision (the LM head scored after every recurrence, losses summed with a fixed schedule), trained on the language domain fixture with the same budget as DN01 (300 epochs, 90 windows). In lib/recur.mlpl: the recurrence over a shared block by nested apply (finding F3: chain copies, apply shares; F6 and F15: repeat traces with literal and parameter-bound counts), the deep-supervision loss, and the per-recurrence state capture. demos/recurrent_block.mlpl trains R in {1, 2, 3, 4} at fixed parameters (the same 3,812 as DN01 plus nothing), records per-recurrence states of an example window with emit_frame (recur/state/r, recur/loss/r, recur/accuracy/val), draws the what/why/how diagrams (the block reused R times with the state passing through; accuracy and validation loss against R at fixed parameters; the per-recurrence prediction of one window improving or not), and writes one results row per R (RC01@1 .. RC01@4) with params, active params per token (R times the block), ms per token, val loss, family accuracies. Gate script with write/check modes (deterministic), catalog entry RC01 (from-scratch: recurrence is the lesson), docs/experiments/RC01.md as a laboratory report answering "does repeated compute replace parameters?", results rows, the recording pinned for the generic host, tests over the recurrence (R=1 equals DN01's forward), the deep-supervision loss, and state capture. Update docs/concepts/recurrence.md status.
