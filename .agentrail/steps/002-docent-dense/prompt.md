CD01 docent dense classifier. In lib/docent.mlpl: the hashed feature encoder (lowercase, word tokens and adjacent bigrams, FNV-style hash modulo a bounded table of 256 to 512 slots, pooled embedding), one hidden layer, an intent head and a destination head, masked losses summed, greedy prediction, top-3, and per-intent and per-destination accuracy. demos/docent_dense.mlpl trains on corpus A in batch (deterministic, bounded, seconds), draws the feature-hashing what/why/how diagram (one query through tokens, bigrams, hash slots, embedding rows, pooled vector), the model diagram, and the run diagram (loss, intent and destination accuracy over epochs, confusion by destination, three decoded queries including an ambiguous and an unsupported one), and writes the results row: params, active params, bytes at f64 and INT8 projection, ms per query, intent accuracy, destination accuracy, top-3, held-out per destination. Gate script with write/check modes, catalog entry CD01, docs/experiments/CD01.md, results table row, tests over the encoder (determinism, bounded slots, bigram handling, unseen words), the heads, and the evaluation.
