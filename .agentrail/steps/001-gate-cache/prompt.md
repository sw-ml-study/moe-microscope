Gate cache. The full gate (just check) takes about 35 minutes because every lesson retrains on every run, and it has been run several times a day for documentation-only commits. Make the gate incremental without weakening it: each demo gate script (scripts/run-*-demo, run-scale-sweep, run-benchmark, run-teacher, run-walkthrough-export, run-recording-parity) computes a cache key from the sha256 of its demo source, the library files it includes (transitively), its fixtures, the results-row and preview files it checks, the recordings.conf line, and the mlpl binary's --version commit line; when tmp/gate-cache/<script>/<key> exists the script prints PASS (cached) and exits 0; a passing run writes the key; write mode never uses the cache; just check --fresh (or GATE_FRESH=1) ignores the cache; the cache lives under tmp/ (ignored). Add just check-docs for documentation-only commits (structure, links, style, catalog, page, landscape) and state in AGENTS.md when it is acceptable (no change under lib/, demos/, tests/, probes/, scripts/run-*, fixtures/, or the binary). Prove the cache is keyed correctly with a test that edits a library file and sees the dependent lessons re-run. Update docs/overview/architecture.md and README build-and-run text.