Gate cache landed and pushed (0dcf465). scripts/gate-cache is sourced by all 20 single-entry demo gate scripts and run-recording-parity (one entry per lesson); keys hash demo source, transitive include closure, path literals under fixtures/docs/assets/catalog, checked previews and fixtures, results rows, recordings.conf line, the script, the helper, and the mlpl version+commit line (mlpl-serve hashed as bytes). Check hits print PASS <script> (cached); passing check runs record the manifest under tmp/gate-cache; failing runs record nothing; write mode never touches the cache; just check --fresh / GATE_FRESH=1 bypass. scripts/check-gate-cache (in the gate, 4 s) proves keying in a scratch copy: library edit reruns only dependents. just check-docs added; AGENTS.md states when it is acceptable. Measured: cold gate 2495 s, warm 51 s with 27 cached entries; a concurrent sibling rebuild of mlpl-repl invalidated all entries once, as intended. Also fixed check-landscape orphaning its http.server (subshell) which made the script exit 1 after PASS. Open: scripts/run-emit-frame-loops has been an empty 0-byte file since commit 28478d6 (F16 flip), so the gate's emit-frame probe passes trivially; needs restoring in its own step.