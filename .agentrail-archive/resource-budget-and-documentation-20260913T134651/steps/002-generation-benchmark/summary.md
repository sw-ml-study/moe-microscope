GB01 delivered: measured build, cold and warm TTFT (encoding excluded), prefill and generation throughput, p50/p95/max token latency for DN01, MX01, MX02, LD01 on freshly built weights; opt-in measurement to a committed fixture with the binary label; deterministic render to docs/reference/generation-benchmark.md validated by the gate; lib/bench.mlpl tested. Committed run: about 5,960 / 3,170 / 2,020 / 3,110 tokens per second, p95 0.19 / 0.34 / 0.59 / 0.35 ms. Committed and pushed.