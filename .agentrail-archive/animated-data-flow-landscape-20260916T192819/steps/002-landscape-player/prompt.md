Landscape player. Extend the live demo under learn/ with a generic panning stage: learn/landscape.html and learn/landscape.js (static, no library) render a scene list from fixtures/landscape/scenes-v0.json, where each scene names its recording, the observation names it reads, the stations, and the chip moves; the player fetches the pinned recordings, lays the landscape out wider than the viewport, pans the camera scene by scene (play, pause, previous, next, a scrubber, and a reduced-motion mode that jumps between scenes), tweens chips between stations from recorded values only, and draws parallel call-outs as labeled lanes. No mechanism semantics in the player: it knows recordings, scenes, stations, and chips, not routers or tables. A ?verify=1 mode lists, per scene, every recording value it read and reports missing observations as errors. scripts/build-site includes the page and the scenes file; scripts/check-learn checks the new files reference committed recordings; the page carries the same footer as the live demo. Verify locally over the composed site with headless Chrome and record the check in docs/results/live-demo.md. Tests: a Node-free check is enough (the gate is shell and MLPL); keep the JavaScript small and readable.