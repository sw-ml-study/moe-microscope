Landscape storyboard written (docs/overview/landscape-storyboard.md): eight scenes in forward-pass order over the walkthrough window and the lessons' seven-position window with a labeled seam, the recording and observation names each scene reads, chip vocabulary and camera path, five parallel call-outs each tied to a recorded value or a cited mechanism property, honest placeholders for the cache, decode-cache compression, and multi-token prediction (MT01), and a per-scene acceptance rule; linked from the docs index, the plan, and the saga queue. Also in this step, answering the user's questions in the documents: the Engram concept page gained a worth-it verdict table, why the table has not paid off and how it could, the n-gram model contrast, the n-gram order question, and the relation to MTP; NG01, EG02, and MT01 queued at the head of the frontier saga; the results table's GitHub rendering fixed (a stale paragraph split the table). Gate passed; commit 98e0d57 pushed.