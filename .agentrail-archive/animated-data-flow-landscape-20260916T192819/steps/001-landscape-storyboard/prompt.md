Landscape storyboard. Write docs/overview/landscape-storyboard.md before any drawing: the ordered scenes of the animated data-flow landscape (tokens and the embedding, attention, the Engram lookup, the router and the four experts with hot and dispatched rows, the recurrence loop, the composition, and placeholders for the expert cache, decode-cache compression, and multi-token prediction until those lessons pin recordings), what moves in each scene and between which stations, the pinned recording and the exact observation names each scene reads (walkthrough-v0 for tokens, hidden, router logits, probabilities, mask, groups, expert rows; engram-run-v0 for rows, retrieved values, gate; recurrent-moe-run-v0 and recurrent-moe-engram-run-v0 for the per-recurrence expert and gate strips and loads; dense-baseline-run-v0 for attention), the parallel call-outs and which recorded values justify them, the chip vocabulary (a token chip, a row chip, a value chip, an expert lane) and the camera path, a per-scene acceptance rule (the values a verifier reads back from the recording), and the rule that nothing animates that a recording does not hold. Add the storyboard to docs/README.md and the plan's Saga 6 section; no code in this step.