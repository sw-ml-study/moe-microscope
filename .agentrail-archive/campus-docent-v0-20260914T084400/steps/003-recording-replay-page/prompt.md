Recording replay page: the first live demo of this repository, published from GitHub Pages and linked from the README (first publication milestone). Build a static page under learn/ (index.html, vanilla JavaScript, no framework, no build step) that fetches the pinned recordings fixtures/recordings/dense-baseline-run-v0.json, moe-top1-run-v0.json, and moe-top2-run-v0.json and steps through them frame by frame: for each step show the recorded values by their names (router logits, probabilities, mask, gate, per-expert loads, balance, entropy, train and val loss) drawn as small tables and bars from the recorded values only, with what/why/how captions taken from the lesson pages; previous, next, play, and a frame index; a lesson picker. Add a dispatch walkthrough panel for SD01 on one recorded batch: tokens grouped per expert, the expert run once, the scatter back, the exact sum, beside the dense-masked path, using values exported by a small MLPL script into fixtures/recordings/dispatch-walkthrough-v0.json (write mode) and checked by the gate. Add the GitHub Pages workflow (.github/workflows/pages.yml publishing learn/ plus the fixtures it reads), a just recipe to serve learn/ locally, and a gate check that learn/ references only committed files. Link the live demo from the README and docs/README.md. Verify in a browser after the deploy that every page loads and every recording steps to its last frame; record the verification (URL, date, frames stepped, any console errors) in docs/results/live-demo.md. No lesson-specific semantics in JavaScript beyond naming and layout; the recordings are the source of truth. Update the saga queue and the experiments index (recordings page).
