MX01 routing delivered: lib/moe.mlpl (router logits, probabilities, top-1 mask inside the loss, Switch gate, top-k masks, renormalized gates, load, entropy, balance loss) with five tests; routing microscope demo with moe/router/* and moe/dispatch/* observations and an annotated routing-transformation diagram, freshness-checked in just check; catalog MX01 (partial). docs/moe-engram-discussion.md captured with plan consequences. F9 verified fixed upstream, pin flipped; DN01 bundle hash re-pinned after the shared SVG helper change. Committed and pushed.