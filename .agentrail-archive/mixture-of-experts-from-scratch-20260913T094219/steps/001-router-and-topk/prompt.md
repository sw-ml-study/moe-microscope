MX01 part 1: implement the router in lib/moe.mlpl as readable MLPL: router linear layer over the hidden state, softmax, top-1 mask via argmax/one_hot inside the loss (F5 resolved), the Switch-style gate softmax(logits) * mask, and the dispatch table [T, E]. Record moe/router/logits, moe/router/mask, moe/router/gate, and moe/dispatch/table as observations from a small deterministic fixture, and draw annotated what/why/how diagrams of router logits, mask, and gate (routing-transformation diagram). Native mlplunit tests first (gate sums, mask one-hot, dispatch counts, gradient reaches the router). Extend just check with the demo freshness check and a catalog entry. No training yet.