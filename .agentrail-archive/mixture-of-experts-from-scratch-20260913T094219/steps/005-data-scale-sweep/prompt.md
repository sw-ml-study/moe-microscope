DS01 data-scale sweep, opt-in and never in the gate: train DN01 and MX01 at 120, 480, and 960 examples (mixture ratios unchanged, generated deterministically from the fixture with total overridden) under a fixed epoch budget chosen so the largest run takes a few minutes, and record validation masked loss and per-family exact-match accuracy against data size and against epochs. Store the measured points as a small committed fixture (fixtures/scale/) so the diagram (validation loss and accuracy versus data size, DN01 beside MX01) is regenerated from the fixture without retraining and freshness-checked in just check; a write mode reruns the sweep. Append results rows whose config names the data size. State plainly whether more data or more epochs moves the held-out columns, and note the cost per run. Tests first for the sweep harness; extend just check with the fixture and diagram checks.