DS01 data-scale sweep delivered: trainer demo, opt-in eight-point sweep script with hash-pinned fixtures/scale index and eight results rows, gate-time diagram redrawn from the points without training, scale helpers with tests. Measured: data cuts validation loss by two thirds (dense 3.48 to 1.11, MoE 3.49 to 1.22 from 120 to 960 examples) and solves prose held-out; doubling epochs at 120 examples worsens it; MoE beats dense at 480, loses at 960, at twice the cost. F15 and F18 verified fixed upstream, pins flipped. Committed and pushed.