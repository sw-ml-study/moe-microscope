MX01 part 2: train the MoE on the synthetic domain with the dense-masked formulation (every expert evaluated, masked gates, exact sparse semantics), 4 to 8 full FFN experts, top-1 routing, plus the load-balance loss and router-entropy metric. Record per-expert load, router entropy, loss curves, and per-family accuracy; measure the memory/speed/quality triple; append the MX01 results row beside DN01; annotated diagrams of the training run and of the load-balance term. Keep the run near DN01's budget (tens of seconds). Tests first; extend just check.