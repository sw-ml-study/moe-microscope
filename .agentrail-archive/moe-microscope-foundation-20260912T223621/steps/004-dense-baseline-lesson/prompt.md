Implement DN01, the dense tiny transformer baseline at microscope scale (vocabulary 64-96, d_model 16-32, one block, T 8-16), as a readable standalone lesson in demos/ built from lib/ modules. Record observations with stable slash names via the facade, generate the annotated step strip and data-structure/transformation diagrams from the recorded values, measure the memory/speed/quality triple (param_count, active params per token, ms per step and per token via clock_ms, validation loss, perplexity, per-task exact-match accuracy), append the first results row, add a catalog entry, and freshness-check the preview SVG in just check. Tests first.