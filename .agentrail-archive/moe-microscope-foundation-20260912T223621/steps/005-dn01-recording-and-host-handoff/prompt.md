Record DN01 over the live mlpl-serve SSE path under the peer version-zero recording schema from ../demo-ml-microscope, pin it by hash in fixtures/recordings/ with an index, probe whether emit_frame inside train { } streams every step or only the last on the connect path and record the result in the ledger and findings, and write the ../demo-extensions handoff for rendering the DN01 recording in the generic Rust/Yew/WASM microscope without lesson-specific Rust (sw-checklist applies to that Rust only). Extend just check with the recording check. Do not modify siblings.