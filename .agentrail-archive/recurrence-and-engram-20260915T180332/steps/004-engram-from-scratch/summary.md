EG01 Engram from scratch: lib/engram.mlpl (ngram_hash addressing, gather_rows retrieval, projection, concat gate) with the builtin's shapes and initialization; exact parity with the engram builtin (outputs and gradients differ by zero on trained parameters, both directions, asserted at 1e-9 in tests and measured in every process). Table-size sweep 1024/256/64/16 slots from scratch plus the builtin at 256, one process each: at 90 training windows the table is memorization capacity (val loss 3.93 to 4.67 against DN01 3.79, lower training loss at every size, prose held-out 0.667 only at 1024 slots; collisions 161 of 769 contexts at 1024, 737 at 16); the plan's prediction that prose moves first did not hold, stated in docs/experiments/EG01.md with next candidates. Three diagrams, five rows, recording pinned and replayed as live-demo lesson eg01 (verified locally), finding 10, concept page updated. Upstream: F19 and F20 verified resolved (probes flipped); F22 fix does not cover the reproducer (numbers filed); F23 second form and new F24 pinned and filed. Saga 16 (animated data-flow landscape) added to the plan per the user's request. Gate passed; commit 0c03865 pushed.