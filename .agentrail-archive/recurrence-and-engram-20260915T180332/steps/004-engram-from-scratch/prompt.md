EG01 Engram from scratch, with parity against the builtin. The Engram idea (DeepSeek): hash the last two and three token ids into slots, read learned vectors, project them, and add them to the hidden state through a learned gate. Show both forms as the plan requires: (1) from scratch in lib/engram.mlpl: ngram_hash addressing (the sw-MLPL builtin for the hash itself, since a frozen cross-backend contract is the point), gather_rows retrieval from a param table (differentiable, finding F4), a linear projection, and a learned sigmoid gate, added to the DN01 block's residual stream after attention; (2) the builtin engram / apply_engram / engram_stats path over the same table shape. A parity check: same seed, same table, same window, identical outputs and identical gradients to within 1e-9, asserted in tests and printed. demos/engram_microscope.mlpl trains the dense block plus the from-scratch Engram on the language domain fixture with the DN01 budget, records slot occupancy, gate magnitude, collisions, and per-family accuracy (engram/slots/nonzero, engram/gate/mean, engram/collisions, engram/accuracy/val), and runs the table-size sweep the plan promises (1,024, 256, 64, 16 slots) plotting collisions, gate magnitude, and prose held-out accuracy against slots; diagrams: the Engram data structure (ids, hashed slots, table rows, projection, gate, sum) on one window with bytes per component; the sweep; the parity panel showing both forms and the difference. Rows EG01@1024 .. EG01@16 with params (table bytes separated), active params per token, ms per token, val loss, family accuracies, plus the builtin form's row for the chosen size. docs/experiments/EG01.md answers "does external memory improve loss and tasks, and at what table size?", and states which family moved first (the plan predicts prose). docs/concepts/engram.md status updated. Gate script, catalog entry (implementation both), recording pinned, tests over hashing determinism, retrieval gradient, gate, parity.
