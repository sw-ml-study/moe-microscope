RM01 recurrent mixture: the MX01 block applied R=2 and R=3 times with re-routing at every recurrence, deep supervision, and the balance term per recurrence (one process per R). Change-of-expert share 0.464 (R=2) and 0.381 (R=3); the R=3 mixture reaches validation loss 3.42, the lowest of any 120-example model here, with training exact match 0.43; R=2 is worse than MX01 (4.09); specialization 0.65 at the first recurrence, 0.45 and 0.49 after. Three diagrams composed from committed metrics, rows RM01@2 and RM01@3, recording replayed as live-demo lesson rm01 (verified locally over the composed site). Finding F23 discovered and pinned: a reshape sized from shape() inside grad drops the gradient silently (the first R=2 run collapsed onto one expert); filed in findings, blockers, and handoffs for upstream. Also carried the F16 resolution flip, finding 9 (recurrence), the recurrence concept page, README and live page text for the new recordings. Gate passed; commit 28478d6 pushed to main. Wiki mirror not yet updated (planned for saga-close).