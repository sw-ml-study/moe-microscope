RM01 recurrent MoE: routing over reasoning time. The MX01 mixture (four experts, top-1, balance term) as the recurrent block of RC01: each recurrence re-routes, so a token can change expert between steps. demos/recurrent_moe.mlpl trains R in {2, 3} with the same budget, records per token per recurrence the chosen expert (recur/route/r), the change-of-expert statistic (share of tokens that switch expert between consecutive recurrences), loads per recurrence, entropy, balance, and the family-by-expert map per recurrence; draws the expert sequence per token as a strip (tokens across, recurrences down, cells colored by expert), the change-of-expert statistic against R, and the per-recurrence specialization maps; writes rows RM01@2 and RM01@3. Compare with RC01 at the same R and with MX01 at R=1 in docs/experiments/RM01.md: what recurrence buys a mixture and whether experts specialize by recurrence step as well as by family. Recording pinned; gate script; catalog entry; tests over the re-routing (a different mask per recurrence is possible) and the statistic.
