Optimizer-state isolation (finding F22). adam keeps per-parameter state by name and it survives re-creating a model under the same name, so any lesson that trains two variants in one process contaminated the second run. Audit every demo for more than one training run per process (delta_experts: LD01 then LD01r; docent_wordvec: CD01b fine-tuned then CD01bf frozen; scale_train and any others) and make each run its own process: the gate script loops over variants passing a selector argument, the demo trains exactly one variant per invocation and writes its row and metrics, and a small drawer composes the comparison diagram from the committed metrics or rows where a diagram needs both. Regenerate the affected rows, diagrams, and any recordings, and update the laboratory reports (LD01.md, CD01b.md), docs/results/current-findings.md, quality.md, the docent results table, the results table, the catalog, and the LinkedIn carousel source with the clean numbers, stating in each report what changed and why. Add a gate check that no demo calls adam in more than one training loop per process, or document the per-process rule in AGENTS.md's MoE microscope contract.
